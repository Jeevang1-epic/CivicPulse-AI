# Gemini triage, server-side only. Optional; deterministic fallback runs without it.
GEMINI_API_KEY=
GEMINI_MODEL=

# Firestore persistence, server-side only. Optional; local fallback runs without it.
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY=
FIRESTORE_REPORTS_COLLECTION=reports
